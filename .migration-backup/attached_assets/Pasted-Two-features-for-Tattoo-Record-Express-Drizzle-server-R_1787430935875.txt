Two features for "Tattoo Record" (Express + Drizzle server; React/Vite/Wouter/shadcn client, "Press" theme: warm paper #ECEAE2, ink #111, cobalt #2B37F5 accent, Space Grotesk + Space Mono, sharp 0px corners). Run `npm run check` + `npm run build` after. Keep everything mobile-responsive.

=====================================================
PART A — Make the demo-login buttons work again (LOGIN PAGE ONLY)
=====================================================
Context: the quick-login buttons on client/src/pages/auth.tsx now only prefill the email (since we removed hardcoded passwords), so clicking them + Sign In fails. Also, seed passwords are being rotated, so the buttons can't know the password. Fix with a server-side demo-login endpoint that issues a token by role — no password involved.

A1. server/routes.ts — add:
    POST /api/auth/demo-login
    - GUARD: only work when `process.env.DEMO_MODE === 'true' || process.env.NODE_ENV !== 'production'`. Otherwise return 404 (feature effectively off in prod unless DEMO_MODE is explicitly set).
    - Body: { role: "ARTIST" | "STUDIO" | "ENTHUSIAST" | "ADMIN" }. Validate with Zod.
    - Map role → the canonical demo account email (artist1@ / studio1@ / enthusiast1@ / admin@tattoorecord.com). Look up the user; if found, issue a JWT via generateToken(user.id) and return { token, user } in the SAME shape as /api/auth/login (id, username, email, role, isVerified, verificationStatus). No password check.
    - Apply the existing authLimiter rate limiter.

A2. client/src/pages/auth.tsx — change handleQuickLogin to POST /api/auth/demo-login with { role } (instead of prefilling email), then store auth and redirect to "/" exactly like a normal login. Keep the buttons ONLY on this auth page. Keep them gated behind `import.meta.env.VITE_DEMO_MODE === 'true' || import.meta.env.MODE !== 'production'`. Also remove the same quick-login block from client/src/pages/home.tsx if present (demo login belongs on /auth only).

A3. Docs/.env.example: add DEMO_MODE (server) and VITE_DEMO_MODE (client) with a note: set both to "true" to enable the one-click demo buttons on a deployed instance; leave unset for a normal production launch.

SECURITY NOTE for the summary: when DEMO_MODE is on for a PUBLIC deployment, the "Admin" demo button hands full admin access to anyone. Recommend either (a) only enabling DEMO_MODE on a private/staging URL, or (b) excluding the ADMIN button from the demo set on public demos. Implement (b): render Artist/Studio/Enthusiast demo buttons always (when demo enabled), but only render the Admin demo button when `import.meta.env.MODE !== 'production'` (dev only).

VERIFY: with demo enabled, clicking Artist/Studio/Enthusiast logs straight in (no typing); with DEMO_MODE unset in production, POST /api/auth/demo-login returns 404 and the buttons are hidden.

=====================================================
PART B — Marketing landing page at "/" for logged-out visitors
=====================================================
Goal: logged-out visitors to "/" see a compelling marketing page; logged-in users still see their feed; all CTAs go to /auth.

B1. Routing (client/src/App.tsx + client/src/pages/home.tsx): the "/" route currently renders Home. Make Home render a new <Landing/> when there is no auth token, and the existing feed when logged in. (Or add the branch in App.tsx.) Do NOT gate /auth or other routes.

B2. Create client/src/pages/landing.tsx — a full marketing page in the Press theme, using theme tokens (bg-background, text-foreground, border-border) and the .np (press-nameplate) / .meta / .halftone utilities already in index.css. Sections, top to bottom:
   1. Top bar: "Tattoo Record" wordmark (press-nameplate); right side: "Sign in" (outline) + "Sign up" (cobalt) buttons → both navigate to /auth (sign-up can deep-link to the register tab, e.g. /auth?mode=register).
   2. HERO (dark halftone band, paper text): a mono kicker ("Nº 01 — THE TATTOO COMMUNITY PLATFORM"), a large press-nameplate headline ("Where tattoo culture gets its record."), a one-line subhead, and two CTAs: "Create free account →" (paper/solid) → /auth?mode=register, and "Explore the feed" (outline) → /auth. Leave a placeholder image slot (halftone block) so real tattoo photos can drop in later.
   3. "BUILT FOR EVERYONE IN THE ROOM" — three bordered cards: Artists (portfolio, bookings, flash sales, get discovered), Studios (manage artists, post jobs, grow reputation), Enthusiasts (find artists, book with confidence, save work).
   4. "EVERYTHING IN ONE PLACE" — a ruled feature grid (gap-px on ink bg): Portfolios & Feed, Bookings & Deposits, Direct Messaging, Flash Sales, Studio Job Board, Stories & Reels. (Do NOT list AI features unless VITE_AI_ENABLED is true — keep it consistent with what's actually on.)
   5. Final CTA band (dark halftone): "Put your work on the record." + "Get started — it's free →" → /auth?mode=register.
   6. Footer: wordmark + links (Sign in, Sign up, Explore, Jobs).
   Do NOT put demo-login buttons on this page (they stay on /auth only).

B3. Mobile: all multi-column grids collapse to 1 column on small screens (grid-cols-1 md:grid-cols-3 etc.); hero headline scales down (clamp/responsive text sizes); CTAs stack; no horizontal overflow at 375px. The landing page has no app sidebar/bottom-nav — it's a standalone marketing shell.

B4. auth.tsx: support the ?mode=register deep link so "Sign up" CTAs open the register tab directly.

VERIFY: logged out, "/" shows the landing page (not a redirect); Sign up → /auth register tab; logged in, "/" shows the feed; landing renders clean at 375px with no overflow; no demo buttons on the landing page.

Summarize files changed/added.